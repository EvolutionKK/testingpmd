name: PMD Source Code Analyzer on Push

on: [push]

jobs:
  pmd:
  
    runs-on: ubuntu-latest
    
    steps:
      - name: run-pmd.bat 
        run: pmd.bat check -d ../force-app/main/default/classes/testingpmd.cls -R rulesets/apex/quickstart.xml -f text
